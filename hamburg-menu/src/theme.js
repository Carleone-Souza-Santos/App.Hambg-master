import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: { main: '#ff4305' },
    secondary: { main: '#ff4305' },
  },
  typography: { fontFamily: 'Roboto, sans-serif' },
});

export default theme;
